<p>
  <strong>Title:</strong>
  <%= @test.title %>
</p>

<p>
  <strong>Level:</strong>
  <%= @test.level %>
</p>

<h2>Questions for <%= @test.title %></h2>

<% if @test.questions.any? %>
  <ul>
    <%= render partial: 'questions/question', collection: @test.questions %>
  </ul>
<% else %>
  <p>Вопросов пока нет</p>
<% end %>

<%= link_to "Add a new question", new_test_question_path(@test), class: "btn btn-primary" %> |
<%= link_to "Edit title", edit_admin_test_path(@test) %> |
<%= link_to "Back to Tests", admin_tests_path %>
