<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm mb-4 rounded">
  <div class="container">
    <%= link_to admin_tests_path, class: 'navbar-brand d-flex align-items-center' do %>
      <i class="bi bi-gear-fill me-2"></i>
      <span class="fw-bold">Панель администратора</span>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbarContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="adminNavbarContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 gap-1">
        <li class="nav-item">
          <%= link_to admin_tests_path, class: 'nav-link d-flex align-items-center' do %>
            <i class="bi bi-list-check me-1"></i>Тесты
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to admin_badges_path, class: 'nav-link d-flex align-items-center' do %>
            <i class="bi bi-award me-1"></i>Бейджи
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to admin_gists_path, class: 'nav-link d-flex align-items-center' do %>
            <i class="bi bi-git me-1"></i>Gists
          <% end %>
        </li>
        <li><hr class="dropdown-divider bg-light opacity-50 my-1"></li>
        <li class="nav-item">
          <%= link_to new_feedback_path, class: 'nav-link d-flex align-items-center' do %>
            <i class="bi bi-chat-dots me-1"></i>Обратная связь
          <% end %>
        </li>
      </ul>

      <div class="navbar-nav ms-auto align-items-center gap-2">
        <span class="nav-item nav-link text-light small">
          <i class="bi bi-person-circle me-1"></i>
          <%= t('admin.nav.welcome') %>, <b><%= current_user.email %></b>
        </span>
        <%= link_to root_path, class: 'btn btn-outline-light btn-sm d-flex align-items-center me-2' do %>
          <i class="bi bi-arrow-left-circle me-1"></i>На сайт
        <% end %>
        <%= link_to t('shared.nav.back_to_tests'), tests_path_for_user(current_user), class: 'btn btn-outline-light btn-sm me-2' %>
        <%= button_to t('admin.nav.logout'), destroy_user_session_path, method: :delete, class: 'btn btn-danger btn-sm' do %>
          <i class="bi bi-box-arrow-right me-1"></i>Выйти
        <% end %>
      </div>
    </div>
  </div>
</nav>

<div class="container mb-3">
  <h2 class="text-center text-primary fw-bold"><i class="bi bi-speedometer2 me-2"></i><%= t('admin.nav.admin_panel') %></h2>
</div>
