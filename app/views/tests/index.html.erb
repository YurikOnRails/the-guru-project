<div class="container mt-4">
  <h1 class="mb-4 text-center"><%= t('.header') %></h1>

  <div class="card shadow-sm">
    <div class="card-body">
      <table class="table table-striped table-hover">
        <thead class="table-primary">
          <tr>
            <th><%= t('.title') %></th>
            <th><%= t('.level') %></th>
            <th><%= t('.category') %></th>
            <th><%= t('.questions_count') %></th>
            <th colspan="2" class="text-center"><%= t('.actions') %></th>
          </tr>
        </thead>
        <tbody>
          <%= render @tests %>
        </tbody>
      </table>
    </div>
  </div>
</div>
