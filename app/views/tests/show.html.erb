<p>
  <strong>Title:</strong>
  <%= @test.title %>
</p>

<p>
  <strong>Level:</strong>
  <%= @test.level %>
</p>

<h2>Questions for <%= @test.title %></h2>

<% if @test.questions.any? %>
  <ul>
    <%= render @test.questions %>
  </ul>
<% else %>
  <p>No questions yet.</p>
<% end %>

<%= link_to "Add a new question", new_test_question_path(@test) %> |
<%= link_to "Edit title", edit_test_path(@test) %> |
<%= link_to "Back to Tests", tests_path %>
