<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.category.name %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t('.show'), test_path(test) %></td>
  <td><%= button_to t('.start'), start_test_path(test), class: 'start-button', method: :post %></td>
</tr>
